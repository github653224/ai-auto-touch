#==============================================================================
# AI Auto Touch - 环境配置文件
#==============================================================================
# 说明: 复制此文件为 .env 并根据实际情况修改配置
# 命令: cp .env.example .env
#==============================================================================

#------------------------------------------------------------------------------
# 项目基本配置
#------------------------------------------------------------------------------
# 后端服务监听地址（0.0.0.0 表示监听所有网络接口）
HOST=0.0.0.0

# 后端服务端口
PORT=8001

# 调试模式（开发环境设为 True，生产环境设为 False）
DEBUG=True

# 工作进程数（生产环境建议设为 CPU 核心数）
WORKERS=1

#------------------------------------------------------------------------------
# ADB 和 scrcpy 配置
#------------------------------------------------------------------------------
# ADB 命令路径（如果已添加到系统 PATH，保持默认即可）
ADB_PATH=adb

# scrcpy 命令路径（如果已添加到系统 PATH，保持默认即可）
SCRCPY_PATH=scrcpy

# 如果 ADB 未在 PATH 中，可以指定完整路径，例如:
# macOS: ADB_PATH=/Users/your-username/Library/Android/sdk/platform-tools/adb
# Linux: ADB_PATH=/home/your-username/Android/Sdk/platform-tools/adb
# Windows: ADB_PATH=C:\Users\your-username\AppData\Local\Android\Sdk\platform-tools\adb.exe

#------------------------------------------------------------------------------
# AI 模型服务配置
#------------------------------------------------------------------------------
# 支持两种部署方式：
# 1. 远程 API 服务（推荐新手）- 无需本地部署，配置简单
# 2. 本地部署（推荐有 GPU 的用户）- 更快的响应速度，更好的隐私保护

#------------------------------------------------------------------------------
# 方式一：使用智谱 AI BigModel 服务（推荐）
#------------------------------------------------------------------------------
# 1. 注册账号: https://open.bigmodel.cn/
# 2. 获取 API Key
# 3. 取消下面三行的注释并填入你的 API Key

AUTOGLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
AUTOGLM_MODEL_NAME=autoglm-phone
AUTOGLM_API_KEY=your-api-key-here

#------------------------------------------------------------------------------
# 方式二：使用 ModelScope 服务
#------------------------------------------------------------------------------
# 1. 注册账号: https://modelscope.cn/
# 2. 获取 API Key
# 3. 取消下面三行的注释并填入你的 API Key

# AUTOGLM_BASE_URL=https://api-inference.modelscope.cn/v1
# AUTOGLM_MODEL_NAME=ZhipuAI/AutoGLM-Phone-9B
# AUTOGLM_API_KEY=your-modelscope-api-key

#------------------------------------------------------------------------------
# 方式三：使用本地部署的模型服务
#------------------------------------------------------------------------------
# 前提条件:
#   - 已安装 vLLM: pip install vllm>=0.12.0 transformers>=4.56.0
#   - 有 NVIDIA GPU（显存 ≥ 24GB）
#   - 已启动模型服务: bash start_model.sh
# 
# 取消下面三行的注释:

# AUTOGLM_BASE_URL=http://localhost:8000/v1
# AUTOGLM_MODEL_NAME=autoglm-phone-9b
# AUTOGLM_API_KEY=EMPTY

#------------------------------------------------------------------------------
# 方式四：使用其他 OpenAI 兼容 API
#------------------------------------------------------------------------------
# 支持任何 OpenAI 格式的 API 服务

# AUTOGLM_BASE_URL=https://your-api-endpoint.com/v1
# AUTOGLM_MODEL_NAME=your-model-name
# AUTOGLM_API_KEY=your-api-key

#------------------------------------------------------------------------------
# AI 模型执行参数
#------------------------------------------------------------------------------
# 最大执行步数（单个任务的最大操作次数）
AUTOGLM_MAX_STEPS=100

# main.py 路径（可选，默认会自动检测）
# 如果自动检测失败，可以手动指定完整路径
# AUTOGLM_MAIN_PY_PATH=/path/to/ai-auto-touch/Open-AutoGLM/main.py

#------------------------------------------------------------------------------
# 设备管理配置
#------------------------------------------------------------------------------
# 最大支持的设备数量
MAX_DEVICES=100

# 截图间隔（秒）
SCREENSHOT_INTERVAL=1

#------------------------------------------------------------------------------
# 日志配置（可选）
#------------------------------------------------------------------------------
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# 日志文件路径
# LOG_FILE=logs/app.log

#------------------------------------------------------------------------------
# 安全配置（生产环境）
#------------------------------------------------------------------------------
# CORS 允许的源（多个源用逗号分隔）
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# API 密钥（可选，用于 API 认证）
# API_KEY=your-secret-api-key

#==============================================================================
# 配置说明完毕
#==============================================================================
# 
# 快速开始:
# 1. 复制此文件: cp .env.example .env
# 2. 选择一种 AI 模型部署方式并配置
# 3. 启动服务: bash start_backend.sh
# 
# 更多帮助请查看 README.md
#==============================================================================
